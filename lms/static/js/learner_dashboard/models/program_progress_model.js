/**
 * Model for User's Program Status.
 */
(function (define) {
    'use strict';
    define([
            'backbone'
        ], 
        function (Backbone) {
        return Backbone.Model.extend({
        });
    });
}).call(this, define || RequireJS.define);
